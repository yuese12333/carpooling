# 后端环境变量示例（用于阿里云短信/号码认证 SDK）
#
# 注意：
# 1. `.env` 会被加入 `.gitignore`，不要提交到远程仓库。
# 2. 需要把下面的 AK/SK 替换成你自己的。
#
# 阿里云 AccessKey
ALIBABA_CLOUD_ACCESS_KEY_ID=
ALIBABA_CLOUD_ACCESS_KEY_SECRET=

# 服务端监听（保持与代码默认一致即可）
PORT=3000
HOST=0.0.0.0

